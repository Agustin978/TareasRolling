let numero;

numero = prompt("Introducir un valor:");

if(parseInt(numero) % 2 == 0)
{
    document.write("El numero ingresado "+numero+" es par.");
}else
{
    document.write("El numero ingresado "+numero+" es impar.");
}
